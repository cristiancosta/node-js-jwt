// Constants.
const errorMessage = require('./error-message');
const httpStatusCode = require('./http-status-code');
const jwtAlgorithm = require('./jwt-algorithm');
const modelName = require('./model-name');
const tableName = require('./table-name');
const tokenSubject = require('./token-subject');

module.exports = {
  errorMessage,
  httpStatusCode,
  jwtAlgorithm,
  modelName,
  tableName,
  tokenSubject
};
